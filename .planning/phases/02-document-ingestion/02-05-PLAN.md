---
phase: 02-document-ingestion
plan: 05
type: execute
wave: 4
depends_on:
  - "02-04"
  - "01-03"
files_modified:
  - src/services/parsing/chunker.ts
  - src/services/retrieval/embedding-service.ts
  - src/services/retrieval/vector-index-service.ts
  - src/services/retrieval/chunk-indexer.ts
  - src/features/import-workbench/components/import-job-list.tsx
  - src/workers/embedding.worker.ts
autonomous: true
requirements:
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - "Imported documents can be chunked and prepared for embeddings after parsing"
    - "The app can persist chunk and vector index records for later retrieval"
    - "User can see import progress through parsing, chunking, embedding, and indexing states"
  artifacts:
    - path: "src/services/parsing/chunker.ts"
      provides: "Document chunk generation"
      min_lines: 40
    - path: "src/services/retrieval/chunk-indexer.ts"
      provides: "Chunk persistence and indexing orchestration"
      min_lines: 40
    - path: "src/services/retrieval/vector-index-service.ts"
      provides: "Voy index persistence wrapper"
      min_lines: 40
  key_links:
    - from: "src/services/parsing/chunker.ts"
      to: "src/services/retrieval/chunk-indexer.ts"
      via: "chunk generation pipeline"
      pattern: "chunk"
    - from: "src/services/retrieval/chunk-indexer.ts"
      to: "src/services/retrieval/vector-index-service.ts"
      via: "index persistence"
      pattern: "index|voy|vector"
user_setup: []
---

<objective>
完成片段切分、向量化和索引入库可视化。

Purpose: 这是从“导入文件”走向“可检索知识底座”的最后一步，Phase 2 到这里才算真正闭环。
Output: chunker、embedding worker、vector index service、chunk indexer 和 import status 可视化。
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/INDEXEDDB-SCHEMA.md
@.planning/research/DOCUMENT-PIPELINE.md
@.planning/phases/01-runtime-foundation/01-03-SUMMARY.md
@.planning/phases/02-document-ingestion/02-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 实现文档切块器</name>
  <files>src/services/parsing/chunker.ts</files>
  <read_first>src/shared/types/document.ts, .planning/research/DOCUMENT-PIPELINE.md</read_first>
  <action>实现 chunker，将 section/clause/paragraph/table/spreadsheet 行转成可嵌入的 DocumentChunk。切块时保留 nodeId、assetId、documentId、sourceRefs 和 tokenEstimate，不要只输出裸文本。</action>
  <verify>rg -n \"DocumentChunk|sourceRefs|tokenEstimate|nodeId\" src/services/parsing/chunker.ts</verify>
  <acceptance_criteria>
    - chunker.ts outputs chunk records with nodeId and sourceRefs
    - chunker.ts handles spreadsheet-related chunk kinds
    - tokenEstimate exists on chunk output
  </acceptance_criteria>
  <done>文档节点可被稳定切成检索片段</done>
</task>

<task type="auto">
  <name>Task 2: 实现 embedding 和向量索引服务</name>
  <files>src/services/retrieval/embedding-service.ts, src/services/retrieval/vector-index-service.ts, src/workers/embedding.worker.ts</files>
  <read_first>.planning/research/STACK.md, .planning/research/INDEXEDDB-SCHEMA.md, src/services/parsing/chunker.ts</read_first>
  <action>实现 embedding-service 和 vector-index-service。嵌入层用 Transformers.js 路线，索引层封装 Voy。embedding.worker.ts 负责后台处理，避免主线程阻塞。当前只做最小可用链路，不在本计划中实现复杂召回策略。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - embedding-service.ts references @huggingface/transformers path
    - vector-index-service.ts encapsulates Voy serialization logic
    - embedding.worker.ts exists and handles chunk processing
  </acceptance_criteria>
  <done>系统可在后台对 chunk 执行嵌入与索引构建</done>
</task>

<task type="auto">
  <name>Task 3: 更新导入进度可视化</name>
  <files>src/services/retrieval/chunk-indexer.ts, src/features/import-workbench/components/import-job-list.tsx</files>
  <read_first>src/services/retrieval/embedding-service.ts, src/services/retrieval/vector-index-service.ts, src/features/import-workbench/components/import-job-list.tsx</read_first>
  <action>实现 chunk-indexer，负责在 parse 完成后推进 chunking、embedding、indexing 三个 stage，并把状态写回 import_jobs。更新 import-job-list UI，让用户能清楚看到“解析、切块、嵌入、索引”各阶段。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - chunk-indexer.ts writes staged progress updates
    - import-job-list.tsx renders chunking, embedding, and indexing stages
    - asset status can reach indexed when background work succeeds
  </acceptance_criteria>
  <done>用户可看到导入到索引完成的全流程状态</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` passes
- [ ] 解析完成的文档可生成 chunk records
- [ ] import job 列表可显示 chunking/embedding/indexing 阶段
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 完成从导入到索引可视化的完整闭环
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-ingestion/02-05-SUMMARY.md`
</output>
