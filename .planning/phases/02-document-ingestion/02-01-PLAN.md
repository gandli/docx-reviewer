---
phase: 02-document-ingestion
plan: 01
type: execute
wave: 1
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/features/import-workbench/components/import-dropzone.tsx
  - src/features/import-workbench/components/import-job-list.tsx
  - src/features/import-workbench/stores/import-store.ts
  - src/services/import/sniff-file.ts
  - src/services/import/import-orchestrator.ts
  - src/shared/types/import.ts
autonomous: true
requirements:
  - DOC-00
  - DOC-01
  - DOC-01A
  - DOC-01B
  - DOC-01C
  - DOC-01D
  - DOC-04
must_haves:
  truths:
    - "User can drag files into the import workbench and see a structured import job start"
    - "The app can distinguish pdf, doc, docx, xls, xlsx, txt, and md before parsing"
    - "Unsupported or doc-only flows show clear Chinese guidance instead of failing silently"
  artifacts:
    - path: "src/services/import/sniff-file.ts"
      provides: "File format sniffing"
      min_lines: 40
    - path: "src/services/import/import-orchestrator.ts"
      provides: "Import flow orchestration"
      min_lines: 50
    - path: "src/features/import-workbench/stores/import-store.ts"
      provides: "Import job state management"
      min_lines: 30
  key_links:
    - from: "src/features/import-workbench/components/import-dropzone.tsx"
      to: "src/services/import/import-orchestrator.ts"
      via: "file upload initiation"
      pattern: "import|upload|orchestrator"
    - from: "src/services/import/import-orchestrator.ts"
      to: "src/services/import/sniff-file.ts"
      via: "format routing"
      pattern: "sniff|format"
user_setup: []
---

<objective>
搭建导入工作台与多格式文件分流管线。

Purpose: 让所有后续预览、解析和索引能力都有统一入口，避免每种文件各走一套散乱流程。
Output: 导入页面、文件拖拽入口、导入任务列表、文件格式识别和导入编排器。
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-1-2-IMPLEMENTATION.md
@.planning/research/FRONTEND-STRUCTURE.md
@.planning/research/DOCUMENT-PIPELINE.md
@.planning/research/document-model.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 建立导入工作台页面与任务状态</name>
  <files>src/features/import-workbench/components/import-dropzone.tsx, src/features/import-workbench/components/import-job-list.tsx, src/features/import-workbench/stores/import-store.ts, src/shared/types/import.ts</files>
  <read_first>.planning/research/FRONTEND-STRUCTURE.md, .planning/research/PHASE-1-2-IMPLEMENTATION.md</read_first>
  <action>创建导入工作台基础 UI，包含拖拽区、文件选择入口和导入任务列表。import-store 至少管理 jobs、warnings、selectedAssetId、activeStage。所有用户可见文案使用中文。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - import-store.ts contains jobs and activeStage state
    - import-dropzone.tsx contains drag and file input handling
    - import-job-list.tsx renders job status in Chinese
  </acceptance_criteria>
  <done>用户可看到稳定的导入工作台入口和任务状态列表</done>
</task>

<task type="auto">
  <name>Task 2: 实现文件格式识别与错误提示</name>
  <files>src/services/import/sniff-file.ts, src/shared/types/import.ts</files>
  <read_first>.planning/research/DOCUMENT-PIPELINE.md, .planning/REQUIREMENTS.md</read_first>
  <action>实现 sniff-file.ts，基于扩展名、mimeType 和文件头尽可能识别 pdf/doc/docx/xls/xlsx/txt/md。对 `.doc` 和无法可靠识别的情况返回 structured warning，不直接抛未经处理的异常。</action>
  <verify>rg -n \"pdf|docx|xlsx|warning|unsupported\" src/services/import/sniff-file.ts</verify>
  <acceptance_criteria>
    - sniff-file.ts recognizes pdf, doc, docx, xls, xlsx, txt, and md
    - sniff-file.ts returns warning path for doc conversion guidance
    - no raw UI strings are hardcoded outside defined result shape
  </acceptance_criteria>
  <done>系统可在解析前稳定判断文件类型并给出清晰提示</done>
</task>

<task type="auto">
  <name>Task 3: 落地导入编排器</name>
  <files>src/services/import/import-orchestrator.ts, src/features/import-workbench/stores/import-store.ts</files>
  <read_first>src/services/import/sniff-file.ts, .planning/research/INDEXEDDB-SCHEMA.md, .planning/research/PHASE-1-2-IMPLEMENTATION.md</read_first>
  <action>实现 import-orchestrator，先完成最小链路：创建 asset 记录、写入原始 blob、创建 import_job、按格式路由到后续 adapter 占位。暂时允许 preview/parse 步骤先返回 stub，但 job stage 必须清晰流转。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - import-orchestrator.ts creates import job stage transitions
    - orchestrator persists asset metadata before adapter dispatch
    - doc flow results in conversion guidance instead of parse attempt
  </acceptance_criteria>
  <done>所有导入任务都有统一编排入口和阶段流转</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` passes
- [ ] 导入工作台可选择文件并创建 job
- [ ] `.doc` 文件会显示先转 `.docx` 的明确提示
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 多格式导入入口和分流底座建立完成
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-ingestion/02-01-SUMMARY.md`
</output>
