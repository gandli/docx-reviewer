---
phase: 01-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/services/persistence/db.ts
  - src/services/persistence/migrations.ts
  - src/services/persistence/repositories/assets-repo.ts
  - src/services/persistence/repositories/settings-repo.ts
  - src/services/persistence/repositories/app-meta-repo.ts
  - src/shared/types/persistence.ts
autonomous: true
requirements:
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - "App can persist local settings and asset metadata in IndexedDB"
    - "Database schema is versioned and ready for future migrations"
    - "Local persistence can survive page refresh"
  artifacts:
    - path: "src/services/persistence/db.ts"
      provides: "Dexie database setup"
      min_lines: 40
    - path: "src/services/persistence/migrations.ts"
      provides: "Schema version and migration registration"
      min_lines: 20
    - path: "src/services/persistence/repositories/assets-repo.ts"
      provides: "Asset metadata persistence"
      min_lines: 30
  key_links:
    - from: "src/services/persistence/db.ts"
      to: "src/services/persistence/repositories/assets-repo.ts"
      via: "repository uses shared db instance"
      pattern: "db\\."
    - from: "src/services/persistence/db.ts"
      to: "offline-doc-assistant"
      via: "database name"
      pattern: "offline-doc-assistant"
---

<objective>
建立本地存储底座、IndexedDB schema 和仓储层。

Purpose: 所有文档、预览、解析和索引能力都依赖稳定的本地持久化，这是离线链路的基础。
Output: Dexie 数据库定义、迁移机制、基础 repositories 和持久化类型。
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-1-2-IMPLEMENTATION.md
@.planning/research/INDEXEDDB-SCHEMA.md
@.planning/research/document-model.ts
@.planning/phases/01-runtime-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 落地 Dexie 数据库定义与版本迁移</name>
  <files>src/services/persistence/db.ts, src/services/persistence/migrations.ts, src/shared/types/persistence.ts</files>
  <read_first>.planning/research/INDEXEDDB-SCHEMA.md, .planning/research/PHASE-1-2-IMPLEMENTATION.md, .planning/research/document-model.ts</read_first>
  <action>按照规划文档定义 Dexie 数据库 `offline-doc-assistant`，至少创建 app_meta、settings、assets、asset_blobs、import_jobs、preview_models、parsed_documents、document_nodes、spreadsheet_sheets、chunks、vector_indexes 这些表。把 schema version 和迁移注册逻辑单独放在 migrations.ts，避免全部写死在 db.ts。</action>
  <verify>rg -n \"offline-doc-assistant|assets:|document_nodes:|vector_indexes:\" src/services/persistence/db.ts src/services/persistence/migrations.ts</verify>
  <acceptance_criteria>
    - db.ts contains database name "offline-doc-assistant"
    - db.ts defines all planned tables from INDEXEDDB-SCHEMA.md
    - migrations.ts exists and registers at least version 1 schema
  </acceptance_criteria>
  <done>数据库结构与规划文档对齐，并支持未来升级</done>
</task>

<task type="auto">
  <name>Task 2: 实现基础仓储层</name>
  <files>src/services/persistence/repositories/assets-repo.ts, src/services/persistence/repositories/settings-repo.ts, src/services/persistence/repositories/app-meta-repo.ts</files>
  <read_first>src/services/persistence/db.ts, .planning/research/INDEXEDDB-SCHEMA.md</read_first>
  <action>实现 assets/settings/app_meta 三个基础 repository，提供 create, getById 或 getByKey, updateStatus, listRecent 等最小可用接口。接口名称要清晰，不能让 UI 直接操作 Dexie table。</action>
  <verify>rg -n \"create|update|getBy|listRecent\" src/services/persistence/repositories/*.ts</verify>
  <acceptance_criteria>
    - assets-repo.ts exposes create and updateStatus
    - settings-repo.ts exposes get/set style methods
    - no React imports exist in repositories
  </acceptance_criteria>
  <done>基础持久化读写可通过仓储层完成</done>
</task>

<task type="auto">
  <name>Task 3: 接通持久化 smoke test 或启动校验</name>
  <files>src/services/persistence/db.ts, src/features/runtime/stores/runtime-store.ts</files>
  <read_first>src/services/persistence/db.ts, src/features/runtime/stores/runtime-store.ts</read_first>
  <action>在应用启动流程中加入一次轻量数据库就绪校验，例如数据库 open、settings round-trip 或 app_meta version 读取。不要写复杂业务，只要能证明 IndexedDB 可用且 schema 初始化成功。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - runtime startup path triggers a db readiness check
    - build passes with persistence layer wired in
    - failure path can distinguish unsupported IndexedDB from generic errors
  </acceptance_criteria>
  <done>应用启动时可确认本地存储底座已经正常可用</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` passes
- [ ] 数据库初始化无运行时报错
- [ ] 刷新页面后 settings 或 app_meta 仍可读回
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- IndexedDB 底座已可支撑后续导入与缓存流程
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-02-SUMMARY.md`
</output>
