---
phase: 01-runtime-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/services/runtime/model-service.ts
  - src/workers/llm.worker.ts
  - src/features/runtime/components/model-status-card.tsx
  - src/features/runtime/stores/runtime-store.ts
  - src/shared/types/model.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - "App can initialize local WebLLM runtime on supported browsers"
    - "User can see model loading, ready, and failure states"
    - "Model runtime configuration survives page refresh via local settings"
  artifacts:
    - path: "src/services/runtime/model-service.ts"
      provides: "WebLLM runtime wrapper"
      min_lines: 50
    - path: "src/workers/llm.worker.ts"
      provides: "Background model loading"
      min_lines: 30
    - path: "src/features/runtime/components/model-status-card.tsx"
      provides: "Model readiness UI"
      min_lines: 30
  key_links:
    - from: "src/features/runtime/components/model-status-card.tsx"
      to: "src/services/runtime/model-service.ts"
      via: "runtime state rendering"
      pattern: "model|load|ready|error"
    - from: "src/services/runtime/model-service.ts"
      to: "src/workers/llm.worker.ts"
      via: "worker-based model initialization"
      pattern: "Worker|postMessage|message"
---

<objective>
接入本地模型加载流程，并把模型状态纳入启动体验。

Purpose: 为后续导入、检索、生成和审阅打下本地 LLM 运行基础，同时把成功/失败路径做清楚。
Output: WebLLM model service、worker 化加载逻辑、模型状态 UI 和本地配置恢复。
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-1-2-IMPLEMENTATION.md
@.planning/research/STACK.md
@.planning/phases/01-runtime-foundation/01-01-SUMMARY.md
@.planning/phases/01-runtime-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 WebLLM 运行时封装</name>
  <files>src/services/runtime/model-service.ts, src/shared/types/model.ts</files>
  <read_first>.planning/research/STACK.md, .planning/research/PHASE-1-2-IMPLEMENTATION.md, src/services/runtime/capability-service.ts</read_first>
  <action>实现 model-service，封装 WebLLM 初始化、模型状态、错误状态、当前模型 ID、缓存恢复所需配置。只做 Runtime Foundation 所需最小能力，不在本计划里实现具体文档生成任务。</action>
  <verify>rg -n \"web-llm|modelId|ready|error|loading\" src/services/runtime/model-service.ts src/shared/types/model.ts</verify>
  <acceptance_criteria>
    - model-service.ts references @mlc-ai/web-llm integration points
    - model.ts exports typed model runtime state
    - service distinguishes loading, ready, and error states
  </acceptance_criteria>
  <done>模型运行时可以被应用层统一使用和观察</done>
</task>

<task type="auto">
  <name>Task 2: 将模型加载移入 Worker 并接通启动流程</name>
  <files>src/workers/llm.worker.ts, src/services/runtime/model-service.ts, src/features/runtime/stores/runtime-store.ts</files>
  <read_first>src/services/runtime/model-service.ts, src/features/runtime/stores/runtime-store.ts, .planning/PROJECT.md</read_first>
  <action>创建 llm.worker.ts，把模型初始化与状态回传放到 Worker 里，避免主线程阻塞。runtime-store 负责在 capability 合格时触发模型准备，并在 IndexedDB 中读取/写回选定模型配置。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - llm.worker.ts exists and handles initialization messages
    - runtime-store wires readiness checks to model loading path
    - selected model configuration persists locally
  </acceptance_criteria>
  <done>模型加载不阻塞 UI，且可在刷新后恢复配置</done>
</task>

<task type="auto">
  <name>Task 3: 提供模型状态面板与失败提示</name>
  <files>src/features/runtime/components/model-status-card.tsx, src/app/App.tsx</files>
  <read_first>src/services/runtime/model-service.ts, src/features/runtime/stores/runtime-store.ts</read_first>
  <action>在启动页增加模型状态卡片，明确显示“未开始、加载中、已就绪、失败”四类状态，并给出失败原因。文案使用中文，不要让用户看到原始异常堆栈作为主信息。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - model-status-card.tsx renders four model states
    - App.tsx shows model status together with runtime readiness
    - error UI displays human-readable Chinese message
  </acceptance_criteria>
  <done>用户能明确知道模型是否已经具备后续流程所需的本地运行条件</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` passes
- [ ] Worker-based model initialization path compiles
- [ ] 刷新页面后仍能读回模型配置与最近状态
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 的本地运行、持久化和模型准备链路完整打通
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-03-SUMMARY.md`
</output>
