---
phase: 01-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/app/App.tsx
  - src/app/main.tsx
  - src/app/router/index.tsx
  - src/features/runtime/stores/runtime-store.ts
  - src/services/runtime/capability-service.ts
  - src/shared/types/runtime.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - "User can open the app and see local runtime readiness status"
    - "User can see whether WebGPU, IndexedDB, File API, and Workers are available"
    - "The frontend project boots successfully in a modern browser"
  artifacts:
    - path: "src/services/runtime/capability-service.ts"
      provides: "Runtime capability detection"
      min_lines: 40
    - path: "src/features/runtime/stores/runtime-store.ts"
      provides: "Runtime readiness state"
      min_lines: 20
    - path: "src/app/App.tsx"
      provides: "Readiness UI entry"
      min_lines: 30
  key_links:
    - from: "src/app/App.tsx"
      to: "src/features/runtime/stores/runtime-store.ts"
      via: "runtime readiness rendering"
      pattern: "runtime|capabilit"
    - from: "src/features/runtime/stores/runtime-store.ts"
      to: "src/services/runtime/capability-service.ts"
      via: "capability detection call"
      pattern: "detect|getCapabilities|capability"
---

<objective>
建立前端项目骨架、运行时能力探测和基础状态管理。

Purpose: 让项目一启动就能明确知道本机浏览器是否具备后续本地 AI 文档处理所需的运行条件。
Output: 可运行的前端工程、能力探测服务、运行时状态 store 和启动页 readiness 面板。
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE-1-2-IMPLEMENTATION.md
@.planning/research/FRONTEND-STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化前端工程骨架与目录</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/app/App.tsx, src/app/main.tsx, src/app/router/index.tsx</files>
  <read_first>.planning/research/PHASE-1-2-IMPLEMENTATION.md, .planning/research/FRONTEND-STRUCTURE.md, .planning/PROJECT.md</read_first>
  <action>创建基于 Vite + React + TypeScript 的前端骨架，建立 app/router/providers/features/services/shared/workers 目录。路由至少包含 `/`、`/import`、`/workspace/:id`、`/settings` 占位入口。避免现在就接入与 Phase 1 无关的业务逻辑。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - package.json contains "vite" and "react"
    - src/app/router/index.tsx contains "/" and "/import" and "/workspace/:id"
    - bun run build exits with code 0
  </acceptance_criteria>
  <done>前端工程可启动并具备后续 Phase 1/2 所需目录骨架</done>
</task>

<task type="auto">
  <name>Task 2: 实现浏览器运行时能力探测服务</name>
  <files>src/services/runtime/capability-service.ts, src/shared/types/runtime.ts</files>
  <read_first>.planning/research/PHASE-1-2-IMPLEMENTATION.md, .planning/PROJECT.md</read_first>
  <action>实现统一的 capability-service，明确输出 WebGPU、IndexedDB、File API、Web Workers、Blob URL、OPFS 可用性字段。定义 RuntimeCapabilityReport 类型，并为不可用场景提供 machine-readable reason 字段，避免只返回布尔值。</action>
  <verify>rg -n \"webgpu|indexeddb|worker|file api|opfs\" src/services/runtime/capability-service.ts src/shared/types/runtime.ts</verify>
  <acceptance_criteria>
    - capability-service.ts contains checks for WebGPU, IndexedDB, Workers, and File API
    - runtime.ts exports RuntimeCapabilityReport
    - capability-service output includes reason fields for unsupported capabilities
  </acceptance_criteria>
  <done>运行时能力可被统一查询并为 UI 消费</done>
</task>

<task type="auto">
  <name>Task 3: 构建运行时状态 store 与 readiness 面板</name>
  <files>src/features/runtime/stores/runtime-store.ts, src/app/App.tsx</files>
  <read_first>src/services/runtime/capability-service.ts, src/shared/types/runtime.ts, .planning/PROJECT.md</read_first>
  <action>实现 runtime-store，负责应用启动时拉取 capability report，并在 App 首页展示 readiness 卡片。界面至少明确区分“可用、不可用、需注意”三类状态，避免只显示原始布尔值。页面文案使用中文。</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - runtime-store.ts calls capability service
    - App.tsx renders runtime readiness status in Chinese
    - App.tsx displays WebGPU and IndexedDB status
  </acceptance_criteria>
  <done>用户打开应用即可看到本机是否满足 Phase 1 运行条件</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install` succeeds
- [ ] `bun run build` passes
- [ ] 打开首页可以看到运行时 readiness 面板
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 前端骨架已满足后续持久化和模型接入需要
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-01-SUMMARY.md`
</output>
